extend layout
block login
	form(method="POST", action="/login")
		input(type="text", placeholder="username", name="user[email]")
		input(type="password", placeholder="password", name="user[password]")
		input(type="submit", value="login")
block content
	h4 #{firstname}
	h4 #{lastname}
	h4 #{email}
